#include "EmitterData.h"


EmitterData::EmitterData( const Vec2f & Position, size_t LaunchCount, size_t MaxParticles )
	:
	position( Position ), launchCount( LaunchCount ), maxParticles( MaxParticles )
{

}